const permissions = {
    VIEW_STATISTIC: 'VIEW_STATISTIC',
    ADD_PHOTOS: 'ADD_PHOTOS',
};

const entities = {
    posts: 'posts',
    comments: 'comments',
};

module.exports = { permissions, entities };
